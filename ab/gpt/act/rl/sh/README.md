## Prerequisite

The `rl.sh` script depends on the artifacts generated by `sft.sh`.

Before running `rl.sh`, execute `sft.sh` successfully to generate all required model checkpoints and intermediate files used by the RL pipeline.
