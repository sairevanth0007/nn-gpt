{
  "improve_classification_only": {
    "comment": [],
    "system": [
      "You are an expert PyTorch neural network architect.",
      "Your task is to generate improved neural network architectures, training hyperparameters, and data transforms that maximise performance on computer vision tasks.",
      "Always follow LEMUR NN Dataset conventions: class name 'Net', a standalone function 'supported_hyperparameters' (outside the class) returning a set of hyperparameter key names, and required methods __init__, forward, train_setup, and learn with their exact signatures.",
      "Output only the requested XML tags (<hp>, <tr>, <nn>) in the correct order — no explanation, no markdown, no extra text."
    ],
    "task": "img-classification",
    "addon_task": null,
    "input_list": [
      {
        "para": "nn_code",
        "value": "nn_code"
      },
      {
        "para": "accuracy",
        "value": "accuracy"
      },
      {
        "para": "epoch",
        "value": "epoch"
      },
      {
        "para": "dataset",
        "value": "dataset"
      },
      {
        "para": "task",
        "value": "task"
      },
      {
        "para": "metric",
        "value": "metric"
      },
      {
        "para": "metric_code",
        "value": "metric_code"
      },
      {
        "para": "transform_code",
        "value": "transform_code"
      },
      {
        "para": "prm",
        "value": "prm"
      }
    ],
    "no_repeat": [],
    "keep_same": [],
    "prompt": [
      "Provide the training hyperparameters (as a JSON object between XML tags <hp> and </hp>), corresponding transformer code (as a Python code between XML tags <tr> and </tr>) and neural network model (as a Python code between XML tags <nn> and </nn>) that achieves a maximum value for the metric '{metric}' after first epoch of training on the '{dataset}' dataset for the task '{task}'.",
      "1. Training hyperparameters (JSON object between XML tags <hp> and </hp>):",
      "  Obligatory keys \"batch\", \"transform\", and all keys from the 'supported_hyperparameters' function in model code. Do not use any other keys like \"hp*\".",
      "  If unsure, use: \"batch\": 64, \"transform\": \"norm_128_flip\".",
      "2. Transformer code (full code between XML tags <tr> and </tr>):",
      "  Use any appropriate transformers available in 'torchvision' package, or your custom implementations.",
      "3. Neural network model (Python code between XML tags <nn> and </nn>):",
      "   It's critical to modify the returned model’s internal architecture to ensure it differs from all models in the LEMUR NN Dataset by:",
      "   Adding, removing, or adjusting existing neural network layers and their arguments.",
      "   Reordering or restructuring layers (e.g., changing dimensions or sequence).",
      "   To generate the new model, use common patterns and ideas from the following reference LEMUR Dataset neural network model and its training configuration, which achieved for a metric '{metric}' value of {accuracy} at epoch {epoch} on the '{dataset}' dataset for the task '{task}' with training hyperparameters <hp>{prm}</hp>, and a code provided in the corresponding tags:",
      "<tr>{transform_code}</tr>",
      "<metric>{metric_code}</metric>",
      "<nn>{nn_code}</nn>",
      "The neural network code inside <nn>...</nn> MUST follow this exact LEMUR interface:",
      "  1. A module-level function (before the class): def supported_hyperparameters(): return {{'lr', 'momentum'}}",
      "     Return a Python set of ONLY the keys you access via prm['key'] or prm.get('key') inside the class body.",
      "     SAFE DEFAULT: return {{'lr', 'momentum'}} — do not add 'weight_decay', 'dropout', 'eps', 'batch_size', 'epochs', 'patience', 'transform', or any other key unless you ACTUALLY call prm['that_key'] in the code. Undeclared-but-used keys cause failure; declared-but-unused keys also cause failure.",
      "     WRONG (validation will reject this): def supported_hyperparameters(): return {{'lr', 'momentum', 'batch_size'}}  — only include 'batch_size' if the class body contains prm['batch_size'] or prm.get('batch_size'). If you do not call it, omit it.",
      "  2. The main model class MUST be named exactly 'Net' and inherit from nn.Module.",
      "     The evaluator calls `module.Net(in_shape, out_shape, prm, device)` by name. A class named ResNet, LeNet, VGGNet, ConvNet, MyModel, or ANYTHING other than 'Net' will cause a NoneType crash. Helper/block classes (e.g. ResidualBlock, SEBlock) may have any name — but the top-level model class is always 'Net'.",
      "     NEVER inherit from torchvision.models classes (ResNet, VGG, AlexNet, etc.) — they have incompatible __init__ signatures. Always inherit directly from nn.Module.",
      "  3. Net.__init__ must accept exactly: (self, in_shape: tuple, out_shape: tuple, prm: dict, device: torch.device) — store self.device = device.",
      "     in_shape is a 4-tuple: (batch_size, channels, height, width). Use in_shape[1] for input channels. NEVER unpack with 3 variables: `c, h, w = in_shape` will crash with ValueError because there are 4 values, not 3.",
      "     Use out_shape[0] for the number of output classes.",
      "  4. Methods train_setup(self, prm) and learn(self, train_data) inside Net.",
      "     train_setup must set self.optimizer and self.criteria as instance attributes (not return them).",
      "     learn receives a DataLoader yielding (inputs, labels) batches — iterate as `for inputs, labels in train_data:`.",
      "  5. Required imports: always include `import torch` and `import torch.nn as nn` at the top.",
      "     If you use F.relu, F.gelu, F.dropout, etc., also add `import torch.nn.functional as F`.",
      "  6. Avoid hardcoded spatial dimensions in Linear layers. Use nn.AdaptiveAvgPool2d((1, 1)) before flattening to make the network input-size agnostic.",
      "  7. Every attribute accessed in forward() (e.g. self.features, self.avgpool, self.fc) MUST be defined in __init__(). Missing self.X definitions cause AttributeError at runtime.",
      "Minimal required structure (adapt the architecture freely — keep this exact skeleton):",
      "import torch",
      "import torch.nn as nn",
      "",
      "def supported_hyperparameters():",
      "    return {{'lr', 'momentum'}}",
      "",
      "class Net(nn.Module):",
      "    def __init__(self, in_shape: tuple, out_shape: tuple, prm: dict, device: torch.device) -> None:",
      "        super().__init__()",
      "        self.device = device",
      "        in_channels = in_shape[1]   # 4-tuple: (batch, channels, H, W)",
      "        num_classes = out_shape[0]",
      "        # ... build your layers here ...",
      "        self.pool = nn.AdaptiveAvgPool2d((1, 1))  # avoids hardcoded spatial dims",
      "        self.fc = nn.Linear(<last_conv_channels>, num_classes)",
      "    def train_setup(self, prm):",
      "        self.to(self.device)",
      "        self.criteria = (nn.CrossEntropyLoss().to(self.device),)",
      "        self.optimizer = torch.optim.SGD(self.parameters(), lr=prm['lr'], momentum=prm['momentum'])",
      "    def learn(self, train_data):",
      "        self.train()",
      "        for inputs, labels in train_data:",
      "            inputs, labels = inputs.to(self.device), labels.to(self.device)",
      "            self.optimizer.zero_grad()",
      "            loss = self.criteria[0](self(inputs), labels)",
      "            loss.backward()",
      "            self.optimizer.step()",
      "    def forward(self, x):",
      "        # ... conv layers ...",
      "        x = self.pool(x)",
      "        x = torch.flatten(x, 1)",
      "        return self.fc(x)",
      "Below is ONE complete, real LEMUR ga- model. Study how it implements the interface above, then design a DIFFERENT architecture of your own — do NOT copy it verbatim.",
      "Reference example (a real LEMUR ga- model):",
      "import torch",
      "import torch.nn as nn",
      "",
      "",
      "def supported_hyperparameters():",
      "    return {{'lr', 'momentum', 'dropout'}}",
      "",
      "",
      "class Net(nn.Module):",
      "    def train_setup(self, prm):",
      "        self.to(self.device)",
      "        self.criteria = (nn.CrossEntropyLoss().to(self.device),)",
      "        self.optimizer = torch.optim.SGD(",
      "            self.parameters(),",
      "            lr=prm['lr'],",
      "            momentum=prm['momentum']",
      "        )",
      "",
      "    def learn(self, train_data):",
      "        self.train()",
      "        for inputs, labels in train_data:",
      "            inputs, labels = inputs.to(self.device), labels.to(self.device)",
      "            self.optimizer.zero_grad()",
      "            outputs = self(inputs)",
      "            loss = self.criteria[0](outputs, labels)",
      "            loss.backward()",
      "            torch.nn.utils.clip_grad_norm_(self.parameters(), 3)",
      "            self.optimizer.step()",
      "",
      "    def __init__(self, in_shape: tuple, out_shape: tuple, prm: dict, device: torch.device) -> None:",
      "        super().__init__()",
      "        self.device = device",
      "        layers = []",
      "        in_channels = in_shape[1]",
      "",
      "        layers += [",
      "            nn.Conv2d(in_channels, 96, kernel_size=11,",
      "                      stride=4, padding=2),",
      "            nn.ReLU(inplace=True),",
      "            nn.MaxPool2d(kernel_size=2, stride=2),",
      "        ]",
      "        in_channels = 96",
      "",
      "        layers += [",
      "            nn.Conv2d(in_channels, 128, kernel_size=3, padding=2),",
      "            nn.ReLU(inplace=True),",
      "            nn.MaxPool2d(kernel_size=2, stride=2),",
      "        ]",
      "        in_channels = 128",
      "",
      "        layers += [",
      "            nn.Conv2d(in_channels, 384, kernel_size=3, padding=1),",
      "            nn.ReLU(inplace=True),",
      "        ]",
      "        in_channels = 384",
      "",
      "        layers += [",
      "            nn.Conv2d(in_channels, 384, kernel_size=3, padding=1),",
      "            nn.ReLU(inplace=True),",
      "        ]",
      "        in_channels = 384",
      "",
      "        layers += [",
      "            nn.Conv2d(in_channels, 192, kernel_size=3, padding=1),",
      "            nn.ReLU(inplace=True),",
      "            nn.MaxPool2d(kernel_size=2, stride=2),",
      "        ]",
      "        in_channels = 192",
      "        self.features = nn.Sequential(*layers)",
      "        self.avgpool = nn.AdaptiveAvgPool2d((6, 6))",
      "",
      "        dropout_p = prm['dropout']",
      "        classifier_input_features = in_channels * 6 * 6",
      "        self.classifier = nn.Sequential(",
      "            nn.Dropout(p=dropout_p),",
      "            nn.Linear(classifier_input_features, 2048),",
      "            nn.ReLU(inplace=True),",
      "            nn.Dropout(p=dropout_p),",
      "            nn.Linear(2048, 2048),",
      "            nn.ReLU(inplace=True),",
      "            nn.Linear(2048, out_shape[0]),",
      "        )",
      "",
      "    def forward(self, x: torch.Tensor) -> torch.Tensor:",
      "        x = self.features(x)",
      "        x = self.avgpool(x)",
      "        x = torch.flatten(x, 1)",
      "        x = self.classifier(x)",
      "        return x",
      "Provide a single COMPLETE Python file. Do not truncate, do not use '...' as a placeholder, do not leave any method body empty. Every opening parenthesis must have a matching closing parenthesis. Do NOT repeat the prompt text or these examples; generate a complete, novel PyTorch model.",
      "Output only three XML tags in this exact order: <hp>...</hp>, <tr>...</tr>, and <nn>...</nn>. Do not wrap them in JSON, do not add markdown fences, and do not add any explanation or extra text."
    ]
  },
  "improve_segmentation_only": {
    "input_list": [
      {
        "para": "nn_code",
        "value": "nn_code"
      }
    ],
    "addon_list": [
      {
        "para": "addon_nn_code",
        "value": "nn_code"
      }
    ],
    "task": "img-segmentation",
    "system": [
      "You are an expert PyTorch neural network architect specialising in image segmentation.",
      "Improve segmentation models while preserving the exact LEMUR interface: a standalone function 'supported_hyperparameters' (outside the class) returning a set of hyperparameter key names, class 'Net', methods __init__, forward, train_setup, and learn with unchanged signatures.",
      "Never use torchvision pre-built models. Provide complete code only."
    ],
    "addon_task": "img-classification",
    "prompt": [
      "Improve the following implementation to make it a better performance on image segmentation tasks given another backbone implementation.",
      "The class name should be 'Net' and you MUST PROVIDE FULL CODES of the changed implementation.",
      "You can change the structure, alternatively also changed training procedure.",
      "Strictly DO NOT remove the `train_setup` and `learn` method, and NEVER change their parameters or implementation.",
      "Strictly NO use of models in `torchvision`.",
      "Focus on improving the model architecture and forward pass only.",
      "Code:\n```\n{nn_code}\n```",
      "Backbone:\n```\n{addon_nn_code}\n```"
    ]
  }
}
